sudo: required
services:
        - docker
before_install:
        - docker build -t reg1reg1/Docker-CI-CD -f Dockerfile.dev .
script:
        - docker run -t reg1reg1/Docker-CI-CD npm run test -- --coverage
